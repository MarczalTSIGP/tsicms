<section class="bg-primary" id="recommendations">
    <div class="container">
        <div class="row">
            <div class="col-lg-12 text-center">
                <h2 class="section-heading text-white">Recomendações</h2>
                <hr class="light my-4">
            </div>
        </div>
    </div>
    <div class="container">
        <div class="row">
            <% @last_recommendations.each do |recommendation| %>
                <div class="col-lg-3 col-md-6 text-center">
                    <div class="service-box mt-5 mx-auto">
                        <h3 class="mb-3 text-white"><%= recommendation.title %></h3>
                        <p class="text-faded mb-4" align="left"><%= recommendation.description %></p>
                    </div>
                </div>
            <% end %>
        </div>
    </div>
     <div class="container">
        <div class="row">
            <div class="col-lg-8 mx-auto text-center">
                <br />
                <br />
                <%= link_to recommendations_path, class: 'btn btn-light btn-xl js-scroll-trigger' do %>
                    Ver Todas as Recomendações
                <% end %>
            </div>
        </div>
    </div>
</section>