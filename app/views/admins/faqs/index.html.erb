<% provide(:page_title, 'Perguntas Frequentes') %>

<div class="text-right">
  <%= link_to new_admins_faq_path,
              class: 'btn btn-secondary',
              title: 'Nova Pergunta',
              data: { toggle: 'tooltip', placement: 'top' } do %>
    <i class="fas fa-plus"></i>
  <% end %>
</div>

<div id="accordion">
  <h4 class="title" >Perguntas</h4>
  <%= render @faqs %>
</div>
<%= paginate @faqs %>
