<% provide(:page_title, 'Empresas') %>

<div class="text-right">
  <%= link_to new_admins_company_path,
              class: 'btn btn-secondary',
              title: 'Nova Empresa',
              data: { toggle: 'tooltip', placement: 'top' } do %>
    <i class="fas fa-plus"></i>
  <% end %>
</div>

<div class="table-responsive">
  <table class="table table-data card-table table-vcenter text-nowrap" id="companies_table">
    <thead>
    <tr>
      <th>Nome</th>
      <th></th>
    </tr>
    </thead>
    <tbody>
    <%= render @companies %>
    </tbody>
  </table>
</div>
